export type TypeType = { id: number, name: string };

export type BrandType = { id: number, name: string };

export type DeviceType = { id: number, name: string, price: number, rating: number, img: string };